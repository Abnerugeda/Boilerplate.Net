Para instalar o boilerplate, faça o clone do projeto e rode o comando:

`dotnet new install ./`

para utilizar em uma solution:
`dotnet new clean-arch -n Ordering -o ./Ordering` 

obs: Ele vai renomear todas as classes library e dependencias para o nome que você escolher no `-n Ordering`  
